<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chunhe.custom.pc.mapper.SysUserDetailMapper">
  <resultMap id="BaseResultMap" type="com.chunhe.custom.pc.model.SysUserDetail">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="store_id" jdbcType="VARCHAR" property="storeId" />
    <result column="code" jdbcType="VARCHAR" property="code" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="job" jdbcType="VARCHAR" property="job" />
    <result column="create_date" jdbcType="TIMESTAMP" property="createDate" />
    <result column="update_date" jdbcType="TIMESTAMP" property="updateDate" />
    <result column="expire_date" jdbcType="TIMESTAMP" property="expireDate" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
    -->
    sud.`id` as `sud_id`, sud.`store_id` as `sud_store_id`, sud.`code` as `sud_code`, 
    sud.`name` as `sud_name`, sud.`job` as `sud_job`, sud.`create_date` as `sud_create_date`, 
    sud.`update_date` as `sud_update_date`, sud.`expire_date` as `sud_expire_date`
  </sql>

  <select id="findSysUserDetailList" resultMap="BaseResultMap">
    SELECT sud.* FROM sys_user_detail sud
    <where>
      1=1
      and sud.expire_date is null
      <if test="storeId != null">
        and sud.store_id = #{storeId}
      </if>
    </where>
  </select>

</mapper>