<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chunhe.custom.pc.mapper.PrintingMapper">
  <resultMap id="BaseResultMap" type="com.chunhe.custom.pc.model.Printing">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="code" jdbcType="VARCHAR" property="code" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="img_url" jdbcType="VARCHAR" property="imgUrl" />
    <result column="create_date" jdbcType="TIMESTAMP" property="createDate" />
    <result column="update_date" jdbcType="TIMESTAMP" property="updateDate" />
    <result column="expire_date" jdbcType="TIMESTAMP" property="expireDate" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
    -->
    p.`id` as `p_id`, p.`code` as `p_code`, p.`name` as `p_name`, p.`img_url` as `p_img_url`, 
    p.`create_date` as `p_create_date`, p.`update_date` as `p_update_date`, p.`expire_date` as `p_expire_date`
  </sql>

  <select id="findPrintingList" resultMap="BaseResultMap">
    SELECT p.* FROM `printing` p
    <where>
      1=1
      and p.expire_date is null
    </where>
  </select>

  <select id="getPrinting" resultMap="BaseResultMap">
    SELECT p.* FROM `printing` p
    <where>
      1=1
      and p.expire_date is null
      <if test="id != null">
        and p.id = #{id}
      </if>
      <if test="code != null">
        and p.code = #{code}
      </if>
      limit 1
    </where>
  </select>

</mapper>