<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chunhe.custom.framework.mapper.SysConfigMapper">
    <resultMap id="BaseResultMap" type="com.chunhe.custom.framework.model.SysConfig">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="key" jdbcType="VARCHAR" property="key"/>
        <result column="value" jdbcType="VARCHAR" property="value"/>
        <result column="remark" jdbcType="VARCHAR" property="remark"/>
        <result column="create_date" jdbcType="TIMESTAMP" property="createDate"/>
        <result column="update_date" jdbcType="TIMESTAMP" property="updateDate"/>
        <result column="expire_date" jdbcType="TIMESTAMP" property="expireDate"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbg.generated
        -->
        sc.`id` as `sc_id`, sc.`name` as `sc_name`, sc.`key` as `sc_key`, sc.`value` as `sc_value`,
        sc.`remark` as `sc_remark`, sc.`create_date` as `sc_create_date`, sc.`update_date` as `sc_update_date`,
        sc.`expire_date` as `sc_expire_date`
    </sql>

    <select id="getSysConfig" resultMap="BaseResultMap">
        SELECT sc.id, sc.name, sc.key, sc.value, sc.remark, sc.create_date FROM `sys_config` sc
        <where>
            1=1
            and sc.expire_date is null
            <if test="id != null">
                and sc.id = #{id}
            </if>
            <if test="key != null">
                and sc.key = #{key}
            </if>
            limit 1
        </where>
    </select>

</mapper>