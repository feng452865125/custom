<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chunhe.custom.pc.mapper.TokenMapper">
    <resultMap id="BaseResultMap" type="com.chunhe.custom.pc.model.Token">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="token" property="token" jdbcType="VARCHAR"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="last_date" property="lastDate" jdbcType="TIMESTAMP"/>
        <result column="expire_date" property="expireDate" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="findToken" resultMap="BaseResultMap">
        select tt.* from `token` tt
        <where>
            1=1
            and tt.expire_date is null
            <if test="token != null">
                and tt.token = #{token}
            </if>
        </where>
        order by tt.id desc
    </select>

    <select id="getToken" resultMap="BaseResultMap">
        select tt.* from `token` tt
        <where>
            1=1
            and tt.expire_date is null
            <if test="token != null">
                and tt.token = #{token}
            </if>
            <if test="userId != null">
                and tt.user_id = #{userId}
            </if>
        </where>
        order by tt.id desc
        limit 1
    </select>

    <update id="setTimeOver" parameterType="com.chunhe.custom.pc.model.Token">
        update `token` set expire_date = now()
        where expire_date is null
        and token != #{token}
        and user_id = #{userId}
    </update>

</mapper>