<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chunhe.custom.pc.mapper.AttachmentMapper">
    <resultMap id="BaseResultMap" type="com.chunhe.custom.pc.model.Attachment">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="img_url" jdbcType="LONGVARCHAR" property="imgUrl"/>
        <result column="enabled" jdbcType="BIT" property="enabled"/>
        <result column="level" jdbcType="INTEGER" property="level"/>
        <result column="remark" jdbcType="VARCHAR" property="remark"/>
        <result column="create_date" jdbcType="TIMESTAMP" property="createDate"/>
        <result column="update_date" jdbcType="TIMESTAMP" property="updateDate"/>
        <result column="expire_date" jdbcType="TIMESTAMP" property="expireDate"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="content" jdbcType="VARCHAR" property="content"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbg.generated
        -->
        a.`id` as `a_id`, a.`name` as `a_name`, a.`img_url` as `a_img_url`, a.`enabled` as `a_enabled`,
        a.`level` as `a_level`, a.`remark` as `a_remark`, a.`create_date` as `a_create_date`,
        a.`update_date` as `a_update_date`, a.`expire_date` as `a_expire_date`, a.`title` as `a_title`,
        a.`content` as `a_content`
    </sql>

    <select id="findAttachmentList" resultMap="BaseResultMap">
        SELECT att.* FROM `attachment` att
        <where>
            1=1
            and att.expire_date is null
            <if test="enabled != null">
                and att.enabled = #{enabled}
            </if>
            <if test="name != null">
                and att.name = #{name}
            </if>
            order by att.level asc, att.create_date desc
        </where>
    </select>

    <select id="getAttachment" resultMap="BaseResultMap">
        SELECT att.* FROM `attachment` att
        <where>
            1=1
            and att.expire_date is null
            <if test="id != null">
                and att.id = #{id}
            </if>
            <if test="enabled != null">
                and att.enabled = #{enabled}
            </if>
            limit 1
        </where>
    </select>

</mapper>