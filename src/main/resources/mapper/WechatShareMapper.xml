<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chunhe.custom.pc.mapper.WechatShareMapper">
    <resultMap id="BaseResultMap" type="com.chunhe.custom.pc.model.WechatShare">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="timestamp" jdbcType="VARCHAR" property="timestamp"/>
        <result column="accessToken" jdbcType="VARCHAR" property="accesstoken"/>
        <result column="ticket" jdbcType="VARCHAR" property="ticket"/>
        <result column="noncestr" jdbcType="VARCHAR" property="noncestr"/>
        <result column="signature" jdbcType="VARCHAR" property="signature"/>
        <result column="create_date" jdbcType="TIMESTAMP" property="createDate"/>
        <result column="last_date" jdbcType="TIMESTAMP" property="lastDate"/>
        <result column="url" jdbcType="VARCHAR" property="url"/>
        <result column="appId" jdbcType="VARCHAR" property="appId"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbg.generated
        -->
        ws.`id` as `ws_id`, ws.`timestamp` as `ws_timestamp`, ws.`accessToken` as `ws_accessToken`,
        ws.`ticket` as `ws_ticket`, ws.`noncestr` as `ws_noncestr`, ws.`signature` as `ws_signature`,
        ws.`create_date` as `ws_create_date`, ws.`last_date` as `ws_last_date`, ws.`url` as `ws_url`, ws.`appId` as `ws_appId`
    </sql>

    <!--取最新一条，判断lastDate是否过期-->
    <select id="getLastWechatShare" resultMap="BaseResultMap">
        SELECT ws.* FROM `wechat_share` ws
        <where>
            1=1
            <if test="appId != null">
                and ws.appId = #{appId}
            </if>
            <if test="url != null">
                and ws.url = #{url}
            </if>
            order by ws.create_date desc
            limit 1
        </where>
    </select>
</mapper>